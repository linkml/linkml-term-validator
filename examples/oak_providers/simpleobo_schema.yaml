id: https://example.org/simpleobo-provider-example
name: simpleobo-provider-example

prefixes:
  TEST: http://example.org/TEST_
  linkml: https://w3id.org/linkml/

default_prefix: simpleobo-provider-example
default_range: string

classes:
  Sample:
    attributes:
      id:
        identifier: true
      term:
        range: TestRootDescendantsEnum

enums:
  TestRootDescendantsEnum:
    reachable_from:
      source_ontology: simpleobo:tests/data/test_ontology.obo
      source_nodes:
        - TEST:0000001
      relationship_types:
        - rdfs:subClassOf
